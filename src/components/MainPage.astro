<section id="home" class="hero-section">
    <div class="hero-content">
        <div class="text-content">
            <h1 class="name">Dharshan<br><span class="surname">Kumar J</span></h1>
            <div class="role-container">
                <span class="static-text">I am a</span>
                <span class="dynamic-text" id="role-text"></span>
                <span class="cursor">|</span>
            </div>
            <!-- <p class="bio">
                Specializing in backend development, API integration, and scalable solutions. 
                Building the digital future with Golang and modern web technologies.
            </p> -->
            <!-- <div class="cta-buttons">
                <a href="#projects" class="btn primary">View Work</a>
                <a href="#contact" class="btn secondary">Contact Me</a>
            </div> -->
        </div>

        <div class="image-content">
            <div class="image-wrapper">
                <img src="/Dharshan.jpg" alt="Dharshan Kumar J" />
                <div class="glow-effect"></div>
            </div>
        </div>
    </div>
</section>

<style>
    .hero-section {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem 0;
    }

    .hero-content {
        display: flex;
        align-items: center;
        justify-content: space-between;
        width: 100%;
        gap: 4rem;
    }

    .text-content {
        flex: 1;
    }

    .name {
        font-size: clamp(3.5rem, 8vw, 5.5rem);
        line-height: 1.1;
        margin-bottom: 1.5rem;
        letter-spacing: -0.02em;
    }

    .surname {
        background: linear-gradient(to right, #fff, var(--text-muted));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .role-container {
        font-family: var(--font-display);
        font-size: 1.5rem;
        color: var(--accent);
        margin-bottom: 1.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-weight: 500;
    }

    .static-text {
        color: var(--text-muted);
    }

    .cursor {
        display: inline-block;
        width: 2px;
        height: 1.2em;
        background-color: var(--accent);
        animation: blink 1s infinite;
    }

    .bio {
        font-size: 1.1rem;
        color: var(--text-muted);
        max-width: 500px;
        margin-bottom: 2.5rem;
        line-height: 1.7;
    }

    .cta-buttons {
        display: flex;
        gap: 1rem;
    }

    .btn {
        padding: 0.8rem 2rem;
        border-radius: 100px;
        font-weight: 600;
        transition: all 0.3s ease;
        font-size: 1rem;
    }

    .btn.primary {
        background: var(--accent);
        color: #fff;
        box-shadow: 0 4px 20px var(--accent-glow);
    }

    .btn.primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 30px var(--accent-glow);
    }

    .btn.secondary {
        background: transparent;
        border: 1px solid var(--border);
        color: var(--text-main);
    }

    .btn.secondary:hover {
        border-color: var(--accent);
        color: var(--accent);
    }

    .image-content {
        flex: 1;
        display: flex;
        justify-content: center;
        position: relative;
    }

    .image-wrapper {
        position: relative;
        width: 400px;
        height: 400px;
        border-radius: 30px;
        z-index: 1;
    }

    .image-wrapper img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 30px;
        filter: grayscale(20%);
        transition: all 0.5s ease;
    }

    .image-wrapper:hover img {
        filter: grayscale(0%);
        transform: scale(1.02);
    }

    .glow-effect {
        position: absolute;
        inset: -20px;
        background: radial-gradient(circle, var(--accent-glow) 0%, transparent 70%);
        z-index: -1;
        opacity: 0.6;
        transition: opacity 0.5s ease;
    }

    .image-wrapper:hover .glow-effect {
        opacity: 1;
    }

    @keyframes blink {
        0%, 100% { opacity: 1; }
        50% { opacity: 0; }
    }

    @media (max-width: 968px) {
        .hero-content {
            flex-direction: column-reverse;
            text-align: center;
            gap: 3rem;
        }

        .role-container {
            justify-content: center;
        }

        .bio {
            margin: 0 auto 2.5rem;
        }

        .cta-buttons {
            justify-content: center;
        }

        .image-wrapper {
            width: 300px;
            height: 300px;
        }
    }
    
    @media (max-width: 768px) {
        .hero-section {
            min-height: auto;
            padding: 4rem 0 3rem;
        }
        
        .hero-content {
            gap: 2rem;
        }
        
        .image-wrapper {
            width: 250px;
            height: 250px;
            border-radius: 20px;
        }
        
        .image-wrapper img {
            border-radius: 20px;
        }
        
        .role-container {
            font-size: 1.2rem;
            flex-wrap: wrap;
        }
        
        .glow-effect {
            inset: -10px;
        }
    }
    
    @media (max-width: 480px) {
        .hero-section {
            padding: 3rem 0 2rem;
        }
        
        .name {
            font-size: clamp(2.5rem, 12vw, 3.5rem);
        }
        
        .image-wrapper {
            width: 200px;
            height: 200px;
        }
        
        .role-container {
            font-size: 1rem;
        }
        
        .cta-buttons {
            flex-direction: column;
            width: 100%;
            gap: 0.75rem;
        }
        
        .btn {
            width: 100%;
            text-align: center;
        }
    }
</style>

<script>
    const roleText = document.getElementById('role-text');
    const roles = [
        "Software Developer",
        "API Specialist",
        "Golang Developer",
        "Backend Engineer"
    ];
    
    let roleIndex = 0;
    let charIndex = 0;
    let isDeleting = false;
    let typingSpeed = 100;

    function typeEffect() {
        const currentRole = roles[roleIndex];
        
        if (isDeleting) {
            roleText.textContent = currentRole.substring(0, charIndex - 1);
            charIndex--;
            typingSpeed = 50;
        } else {
            roleText.textContent = currentRole.substring(0, charIndex + 1);
            charIndex++;
            typingSpeed = 100;
        }
        
        if (!isDeleting && charIndex === currentRole.length) {
            isDeleting = true;
            typingSpeed = 2000;
        } else if (isDeleting && charIndex === 0) {
            isDeleting = false;
            roleIndex = (roleIndex + 1) % roles.length;
            typingSpeed = 500;
        }
        
        setTimeout(typeEffect, typingSpeed);
    }
    
    setTimeout(typeEffect, 1000);
</script>